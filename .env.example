# API端点配置
# 开发环境使用本地Workers: http://localhost:8787
# 生产环境使用部署的Workers URL
VITE_API_ENDPOINT=http://localhost:8787

# 直接 API 调用模式（仅用于 GitHub Pages 部署）
# ⚠️ 警告：启用此模式会将 API 密钥暴露在前端代码中
# 仅用于演示，生产环境请使用后端代理
VITE_USE_DIRECT_API=false

# 豆包 API 配置（仅在 VITE_USE_DIRECT_API=true 时需要）
# ⚠️ 警告：此密钥会暴露在前端代码中
VITE_DOUBAO_API_KEY=YOUR_API_KEY_HERE
VITE_DOUBAO_API_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3

# 管理员配置
# 管理员设备ID列表（逗号分隔）
VITE_ADMIN_DEVICES=admin,test_admin

# 管理员密码（可选，用于额外验证）
VITE_ADMIN_PASSWORD=admin123
