# 食物卡路里分析器 - 项目结构

## 📁 项目组成

### 1. 主应用（生产版本）
**位置**: 根目录 `/`  
**访问**: http://localhost:5173  
**特点**:
- 完整功能的生产版本
- 本地 AI 模型异步检测（不阻塞用户）
- 支持历史记录、数据分析、目标管理
- 部署到 Cloudflare Pages
- ✅ 已修复 STORAGE_ERROR 问题

**启动命令**:
```bash
npm run dev          # 开发模式
npm run build        # 构建生产版本
```

### 2. Demo 版本（严格验证）
**位置**: `demo-frontend-only/`  
**仓库**: https://github.com/lucas8848168/Calorie-AnalysisDEMO.git  
**访问**: http://localhost:5175/Calorie-AnalysisDEMO/  
**特点**:
- 演示版本，强调本地 AI 验证
- 本地模型同步等待（严格验证）
- 独立部署，不依赖主应用
- 用于展示 AI 检测能力
- ✅ 从 GitHub 拉取的干净版本

**启动命令**:
```bash
cd demo-frontend-only
npm run dev          # 开发模式（端口 5175）
npm run build        # 构建生产版本
```

### 3. 后端 API
**位置**: `workers/`  
**部署**: Cloudflare Workers  
**功能**: 
- 调用豆包 1.6 Vision API
- 食物识别和营养分析
- 无服务器架构

**启动命令**:
```bash
cd workers
npm run dev          # 本地开发（端口 8787）
npm run deploy       # 部署到 Cloudflare
```

## 🗂️ 目录说明

```
/
├── src/                    # 主应用源码
│   ├── components/         # React 组件
│   ├── services/           # 业务逻辑
│   ├── utils/              # 工具函数
│   └── types/              # TypeScript 类型
│
├── demo-frontend-only/     # Demo 版本（独立，从 GitHub 克隆）
│   └── src/                # Demo 源码
│
├── workers/                # 后端 API
│   └── src/                # Workers 源码
│
├── public/                 # 静态资源
├── dist/                   # 构建输出
├── docs/                   # 文档
└── logs/                   # 调试日志
```

## 🚀 快速开始

### 开发环境
```bash
# 1. 安装主应用依赖
npm install

# 2. 启动主应用
npm run dev

# 3. 启动后端（新终端）
cd workers && npm run dev

# 4. 启动 Demo（可选，新终端）
cd demo-frontend-only && npm install && npm run dev
```

### 生产部署
```bash
# 主应用：自动部署到 Cloudflare Pages（Git 推送触发）
git push origin main

# 后端 API：手动部署
cd workers && npm run deploy

# Demo：独立部署到 GitHub Pages
cd demo-frontend-only && npm run build
```

## 🔧 配置文件

- `.env` - 主应用环境变量
- `demo-frontend-only/.env` - Demo 环境变量
- `workers/.dev.vars` - Workers 本地环境变量
- `workers/wrangler.toml` - Workers 配置

## 📝 主要差异

| 特性 | 主应用 | Demo 版本 |
|------|--------|-----------|
| 本地 AI 检测 | 异步，不阻塞 | 同步，严格验证 |
| 历史记录 | ✅ | ✅ |
| 数据分析 | ✅ | ❌ |
| 目标管理 | ✅ | ❌ |
| 部署方式 | Cloudflare Pages | GitHub Pages |
| 用途 | 生产使用 | 演示展示 |
| 代码来源 | 本地开发 | GitHub 仓库 |

## 🐛 已修复的问题

1. **STORAGE_ERROR**: 修复了模型加载失败时的错误处理（主应用）
2. **本地检测逻辑**: 改进了错误返回值（主应用）
3. **调试日志**: 添加了详细的 emoji 标记日志（主应用）

## 📚 相关文档

- `README.md` - 项目介绍
- `QUICK_REFERENCE.md` - 快速参考
- `DEBUG_GUIDE.md` - 调试指南
- `DEMO_SETUP_COMPLETE.md` - Demo 设置记录
- `logs/storage-error-debug-2024-11-21.md` - 调试日志
- `docs/` - 详细技术文档

## 🔄 版本同步

如果 Demo 版本需要同步主应用的修复：
1. 将主应用的修复代码复制到 Demo
2. 或者在 Demo 仓库中提交 PR
3. 重新从 GitHub 拉取更新
